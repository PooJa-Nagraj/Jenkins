pipeline{
    agent any
    stages{
        stage('checkout'){
            steps{
                git branch : 'main'
                    url: "https://github.com/PooJa-Nagraj/Jenkins.git"
                    credentialsId: 'git-cred'
            }
        }
    }
}